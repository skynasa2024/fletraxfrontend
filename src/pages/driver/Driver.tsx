import { Container } from '@/components/container';

import { DriverPage } from '.';

const Driver = () => {
  return (
    <Container>
      <DriverPage />
    </Container>
  );
};

export { Driver };
